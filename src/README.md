# Source Code

The main classifier is currently maintained in the repository root as `cat dog image classifier.py`.

The implementation provides:

- image loading and preprocessing
- feature extraction
- Random Forest training
- prediction and probability estimation
- evaluation and visualization

The `src` directory is reserved for future modularization as the project grows.
